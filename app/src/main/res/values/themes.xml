<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main app theme (AppCompat) -->
    <style name="Theme.AdMobDemo" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize colors -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.AdMobDemo.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <!-- Optional: Use drawable for splash background
        <item name="android:windowBackground">@drawable/splash_background</item>
        -->
    </style>

</resources>
